# Understanding The Problem

## What do we have?

- payload

    ```json
    {
        "reservation_id": "<a-reservation-id>",
        "status": "CONFIRMED"
    }
    ```
- endpoint name /webhooks/payment
- status can be CONFIRMED or FAILED

## Restrictions

- is the reservation pending for 15 minutes or more it should be considered expired

## Devising a Plan

- are the statuses terminal?
- looks like we need a background job or something

Restate:

    implement an endpoint to receive webhook calls
    have a background process to mark a reservation as expired

we knda have two problems in this big one


### Plan 1

1. Create migration to add two new payment statuses to the ReservationModel (FAILED and EXPIRED)
2. Create a use-case for updating the payment status of the Reservation
    - Receives a DTO
    - Creates a domain object
    - Persists
3. Create wehook routes
    - Validate payload
    - Validate

## Carrying Out the Plan 1

1. Migration
    - we must add new types
    - i just realized knex schema.enum, by default, doesn't use enums but constraints - need to change that
    - since all the data in reservation are pending ones we can easily recreate the column with all pending and this
        should work

## Looking back 1

- webhook/payment is a critical endpoint - should that be authenticated?

---

### Plan 2

1. Have a repeatable job that gets all the reservation that should be marked as expired
2. create the use case that mark reservations as expired (i am tempted to use the same use case as before...)
3. Define a worker handler
    - validate input
    - Call the use case that already exists for updating statuses

## Looking back 2

