
1. Add a 'hello world' that touches all the layers in the application (Pragmatic Programmer Tracer Bullets).
    Implement anything from point to point
    As you walk through the code and existing patterns, take notes about what you think it's weird (i leave TODO
    comments everywhere). You can then compile everything afterwards.

note: do something simples, copy parts of the code and change them - i know you don't have all the time in the world for
doing that

I implemented an endpoint /facts, it allows me to touch:

- fastify validation stuff - which works out of the box.

- added a migration
    npm run knex migrate:make create_facts_of_life
- insert some data

    I got some issue when trying to insert with my new model (it compplain)

>  insert into \"facts_of_life\" (\"answer\", \"createdAt\", \"question\", \"updatedAt\") values ($1, $2, $3, $4) returning \"id\" - column \"createdAt\" of relation \"facts_of_life\" does not exist"

    `docker compose exec db bash` <-- to get into the container
    `psql -U postgres -d park42_development` <-- to get into the database

it turns out the application is kinda broken. base model is adding camelCase dates but using $beforeInsert but this is
being overwritten bu subclasses like User and Session

super.$beforeInsert() should be used. Actually, let me fix the baseModel

the table was created as snake_case. it should be camel case due to the baseModel implementation

fixed the model and the subclasses

2. Do you know any tool that will make your life easier? Take time to add it to the project

Maybe zod to get type safety? maybe some pgadmin to examine data batter in postgres

3. Any pattern that will make your life easier?

- move the authentication stuff to a prehandler
- fastify centralized error handler

4. tests

while writing some tests just realized that the auth check only happens after request comes in, makin a request without
valid payload authenticated or not. i beleieve it would be better to have auth before anything


# Links

https://fastify.dev/docs/latest/Reference/Validation-and-Serialization/
https://fastify.dev/docs/latest/Reference/Hooks/#prevalidation
https://fastify.dev/docs/latest/Reference/Routes/#route-prefixing
